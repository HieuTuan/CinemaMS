spring.application.name=cinemaAI
spring.config.import=optional:file:.env[.properties],optional:file:cinemaAI/.env[.properties],optional:file:BE/cinemaAI/.env[.properties]
server.port=8080
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/cinema_ai}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:123456}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto= update


spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect


spring.flyway.enabled=false
# Database
# spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/cinema_ai?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
# spring.datasource.username=${DB_USERNAME:root}
# spring.datasource.password=${DB_PASSWORD:}
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA
# spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.format_sql=true

# spring.flyway.enabled=false

management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=when_authorized
management.health.mail.enabled=false

springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.tags-sorter=alpha
springdoc.swagger-ui.operations-sorter=method
springdoc.swagger-ui.display-request-duration=true

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:}
spring.mail.password=${MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

app.mail.enabled=${MAIL_ENABLED:true}
app.mail.from=${MAIL_FROM:iwakeupwhen@gmail.com}

vnpay.tmn-code=${VNPAY_TMN_CODE:}
vnpay.hash-secret=${VNPAY_HASH_SECRET:}
vnpay.api-url=https://sandbox.vnpayment.vn/paymentv2/vpcpay.html
vnpay.api-query-url=https://sandbox.vnpayment.vn/merchant_webapi/api/transaction
vnpay.return-url=http://localhost:8080/api/v1/payments/vnpay/return
# Thay YOUR_NGROK_URL bằng URL từ ngrok (VD: https://abc123.ngrok.io)
vnpay.ipn-url=http://localhost:8080/api/v1/payments/vnpay/ipn

app.jwt.secret=${JWT_SECRET:cineai-development-secret-key-please-change-123456}
app.jwt.access-expiration-ms=${JWT_ACCESS_EXPIRATION_MS:900000}
app.jwt.refresh-expiration-ms=${JWT_REFRESH_EXPIRATION_MS:604800000}
app.google.client-id=${GOOGLE_OAUTH_CLIENT_ID:${GOOGLE_CLIENT_ID:}}
app.booking.hold-cleanup.enabled=${BOOKING_HOLD_CLEANUP_ENABLED:true}
app.booking.hold-cleanup.fixed-delay-ms=${BOOKING_HOLD_CLEANUP_FIXED_DELAY_MS:60000}

cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME:}
cloudinary.api-key=${CLOUDINARY_API_KEY:}
cloudinary.api-secret=${CLOUDINARY_API_SECRET:}
spring.servlet.multipart.max-file-size=100MB
spring.servlet.multipart.max-request-size=100MB

# ─── Hybrid Content-Based Recommendation – Signal Weights ──────────────────
# Trailer signals
app.recommendation.weights.trailer-complete=${REC_W_TRAILER_COMPLETE:5.0}
app.recommendation.weights.trailer-view-max=${REC_W_TRAILER_VIEW_MAX:5.0}
app.recommendation.weights.trailer-click=${REC_W_TRAILER_CLICK:1.0}
app.recommendation.weights.trailer-skip=${REC_W_TRAILER_SKIP:-1.0}

# Booking signals
app.recommendation.weights.booking=${REC_W_BOOKING:4.0}
app.recommendation.weights.ticket-count-max=${REC_W_TICKET_COUNT_MAX:1.5}
app.recommendation.weights.ticket-count-saturation=${REC_W_TICKET_COUNT_SATURATION:6}

# Review signals
app.recommendation.weights.review-high-multiplier=${REC_W_REVIEW_HIGH_MULT:1.5}
app.recommendation.weights.review-low-cap=${REC_W_REVIEW_LOW_CAP:-2.0}

# Wishlist signal
app.recommendation.weights.wishlist=${REC_W_WISHLIST:3.0}

# Movie scoring multipliers
app.recommendation.weights.actor-multiplier=${REC_W_ACTOR_MULT:1.2}
app.recommendation.weights.director-multiplier=${REC_W_DIRECTOR_MULT:1.1}
app.recommendation.weights.recency-bonus=${REC_W_RECENCY_BONUS:0.5}
app.recommendation.weights.recency-months=${REC_W_RECENCY_MONTHS:6}
